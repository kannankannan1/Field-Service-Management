# Backend API URL for production builds
# In dev mode (vite proxy handles /api -> localhost:8080), leave this empty
# In production (Vercel), set to your Railway/Render backend URL
# Example: VITE_API_URL=https://your-backend.up.railway.app
VITE_API_URL=
